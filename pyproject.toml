[tool.poetry]
name = "1c-system-programming"
version = "0.1.0"
description = ""
authors = ["fiobond <borisoffficial@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
loguru = "^0.7.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

